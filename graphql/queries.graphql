query GetProductBySlug($slug: String!) {
	product(where: { slug: $slug }) {
		name
		price
		id
		images {
			id
			url
		}
	}
	reviews {
		id
		name
		content
		rating
	}
}

query GetProducts {
	products {
		id
		name
		price
		slug
		images {
			url
		}
	}
}
